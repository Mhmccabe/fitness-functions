name: Fitness Functions — .NET Logging Standards

on:
  push:
    branches: [main]
    paths:
      - 'dotnet/logging-standards/**'
  pull_request:
    branches: [main]
    paths:
      - 'dotnet/logging-standards/**'
  schedule:
    # Weekly full scan every Monday at 06:00 UTC
    - cron: '0 6 * * 1'
  workflow_dispatch:

defaults:
  run:
    working-directory: dotnet/logging-standards

permissions:
  contents: read
  security-events: write

jobs:
  fitness-functions:
    name: Logging Standards Fitness Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 0

      - name: Set up .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9
        with:
          dotnet-version: '8.0'

      - name: Install semgrep
        run: pip install semgrep

      # ── Layer 1: Semgrep — Logging observability standards ─────────────────
      - name: Semgrep — logging rules
        run: |
          semgrep \
            --config .semgrep/logging-rules.yml \
            src/ \
            --exclude="*Bad.cs" \
            --json \
            --output semgrep-results.json \
            --quiet \
            --no-autofix \
            --metrics=off || true
          VIOLATIONS=$(python3 -c "
          import json, sys
          data = json.load(open('semgrep-results.json'))
          count = len(data.get('results', []))
          print(count)
          sys.exit(0)
          ")

      - name: Convert Semgrep results to SARIF
        if: always()
        run: |
          python3 - << 'PYEOF'
          import json

          try:
              data = json.load(open('semgrep-results.json'))
              results = data.get('results', [])
          except Exception:
              results = []

          runs = [{"tool": {"driver": {"name": "Semgrep", "informationUri": "https://semgrep.dev", "rules": []}}, "results": []}]
          rule_ids = {}
          for r in results:
              rule_id = r.get('check_id', 'unknown')
              msg = r.get('extra', {}).get('message', '').splitlines()[0]
              if rule_id not in rule_ids:
                  rule_ids[rule_id] = True
                  runs[0]['tool']['driver']['rules'].append({"id": rule_id, "shortDescription": {"text": msg}, "helpUri": "https://semgrep.dev"})
              runs[0]['results'].append({"ruleId": rule_id, "message": {"text": msg}, "locations": [{"physicalLocation": {"artifactLocation": {"uri": r.get('path', '')}, "region": {"startLine": r.get('start', {}).get('line', 1), "startColumn": r.get('start', {}).get('col', 1)}}}], "level": "warning"})

          sarif = {"version": "2.1.0", "$schema": "https://json.schemastore.org/sarif-2.1.0.json", "runs": runs}
          with open('semgrep.sarif', 'w') as f:
              json.dump(sarif, f, indent=2)
          PYEOF

      - name: Upload Semgrep SARIF to GitHub Code Scanning
        if: always()
        uses: github/codeql-action/upload-sarif@820e3160e279568db735cee8ed8f8e77a6da7818
        with:
          sarif_file: dotnet/logging-standards/semgrep.sarif
          category: semgrep-dotnet-logging

      - name: Upload Semgrep results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: semgrep-results-dotnet
          path: dotnet/logging-standards/semgrep-results.json

      # ── Layer 2: dotnet build and tests ────────────────────────────────────
      - name: Build
        run: dotnet build LoggingStandards.sln --configuration Release

      - name: Run tests with coverage
        run: |
          dotnet test LoggingStandards.sln \
            --configuration Release \
            --collect:"XPlat Code Coverage" \
            --results-directory ./coverage \
            -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: coverage-report-dotnet
          path: dotnet/logging-standards/coverage/**/coverage.opencover.xml

      # ── Layer 3: SonarQube static analysis ────────────────────────────────
      - name: SonarQube scan
        continue-on-error: true
        uses: SonarSource/sonarqube-scan-action@13990a695682794b53148ff9f6a8b6e22e43955e
        with:
          projectBaseDir: dotnet/logging-standards
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      # ── Layer 4: GitHub Actions job summary ───────────────────────────────
      - name: Write job summary
        if: always()
        run: |
          VIOLATIONS=$(python3 -c "
          import json, sys
          try:
              data = json.load(open('semgrep-results.json'))
              print(len(data.get('results', [])))
          except Exception:
              print('N/A')
          ")

          if [[ "$VIOLATIONS" = "0" ]]; then
            VIOLATIONS_STATUS="✅ 0"
          else
            VIOLATIONS_STATUS="⚠️ $VIOLATIONS"
          fi

          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## Fitness Function — .NET Logging Standards

          | Check | Result |
          |---|---|
          | Logging violations (semgrep) | ${VIOLATIONS_STATUS} |

          **Commit:** \`${{ github.sha }}\`
          **Branch:** \`${{ github.ref_name }}\`
          **Run:** [${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          EOF
